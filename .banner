#toilet -f smmono12 CMAX Wireless
if [ -x /usr/bin/figlet -a -x /usr/bin/toilet ]; then
	figlet -ctf slant HULRYUNG!!
else
	echo -e "please install figlet and toilet"
	echo -e "sudo apt-get install figlet toilet"
fi
